import { BackgroudColorDirective } from './backgroud-color.directive';

describe('BackgroudColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroudColorDirective();
    expect(directive).toBeTruthy();
  });
});
